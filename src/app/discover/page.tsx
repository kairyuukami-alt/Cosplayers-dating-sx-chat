import { Nav } from '@/components/nav'

export default function DiscoverPage() {
  return (
    <>
      <Nav />
      <main className="page-shell"><section className="panel"><div className="eyebrow">DISCOVER</div><h1>Matching engine is the next branch.</h1><p>Your authenticated shell and profile are ready. The discovery stack will land separately so it has its own reviewable commit.</p></section></main>
    </>
  )
}
