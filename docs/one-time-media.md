# View-once media

Recipients receive a short-lived signed URL for a view-once image/video. Once consumed, the database records `viewed_at` and normal recipient signing is denied. This is a UX/privacy control rather than DRM: screenshots, screen recordings, cameras, or modified clients cannot be fully prevented.
