# Support channel behavior

The Support tab is separate from dating matches. A platform administrator can initiate a support thread with any registered user, but the thread is visibly labeled as platform support on both sides.
