# Retention

Current MVP behavior does not create a hidden external backup of private chat media.

If a future backup system is introduced, define and disclose its purpose, destination, encryption, access controls, and deletion schedule before enabling it. A three-day backup window should not be implemented invisibly to users.
